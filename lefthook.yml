pre-commit:
  parallel: true
  commands:
    lint-biome:
      run: pnpm run lint:biome
    lint-types:
      run: pnpm run lint:types
    lint-knip:
      run: pnpm run lint:knip

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}